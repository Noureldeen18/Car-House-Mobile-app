import 'package:flutter/material.dart';

class AppColors {
  // الأساسيات
  static const Color primaryBlue = Color(0xFF0A3D62);   // Navy Blue
  static const Color secondaryOrange = Color(0xFFF76C2F); // Bright Orange
  static const Color accentGray = Color(0xFF4A4A4A);    // Steel Gray

  // الخلفيات
  static const Color backgroundLight = Color(0xFFF2F2F2);
  static const Color white = Color(0xFFFFFFFF);

  // النصوص
  static const Color textBlack = Color(0xFF000000);
  static const Color textDarkGray = Color(0xFF555555);

  // الحالات
  static const Color successGreen = Color(0xFF27AE60);
  static const Color errorRed = Color(0xFFE74C3C);

  // إضافات
  static const Color shadow = Color(0x1A000000); // ظل خفيف
}
